

utils/utils:
	local_search(graph,state,batch_size)
	update_function(matrix1, matrix2, matrix3, state, old_conflicts, t, alpha, beta)
	reinforcement_learning(alpha,beta,gamma,graph):
	local_search(graph,state,batch_size):
	cost_function(state,graph)
	generate_state(pmat1,pmat2,pmat3)
